# Set these values in your production EAS environment or CI before building.

# Must point at the deployed HTTPS backend. The app will append /api automatically if omitted.
EXPO_PUBLIC_API_URL=https://api.example.com
EXPO_PUBLIC_MAPTILER_API_KEY=replace-with-maptiler-key
